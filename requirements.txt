# proxAI — runtime dependencies
# Python 3.10+
#
# Setup with venv:
#   python3 -m venv .venv
#   source .venv/bin/activate        # Windows: .venv\Scripts\activate
#   pip install --upgrade pip
#   pip install -r requirements.txt
#
# This file installs the package in editable mode (-e .) so the `proxai` CLI is available.

-e .
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
httpx>=0.28.0
python-dotenv>=1.0.0
pydantic>=2.10.0
pydantic-settings>=2.6.0